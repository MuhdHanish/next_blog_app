/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "img.freepik.com", "cdn.create.vista.com", "avatars.githubusercontent.com", "lh3.googleusercontent.com"],
  }
};

export default nextConfig;
